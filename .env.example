# Public PostHog project ingest configuration. Both are required for delivery.
# Set these in .env.local for local work or the production build environment.
# Use your project's actual ingest host (including self-hosted installations).
# No personal API key belongs here. VITE values are visible in the built app.
VITE_POSTHOG_HOST=
VITE_POSTHOG_PROJECT_TOKEN=
