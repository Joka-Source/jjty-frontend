export const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const paths = {
 home:'m3 10 9-7 9 7v10a1 1 0 0 1-1 1h-5v-7H9v7H4a1 1 0 0 1-1-1z',
 search:'M21 21l-5-5M18 10a8 8 0 1 1-16 0 8 8 0 0 1 16 0',
 grid:'M3 3h7v7H3zM14 3h7v7h-7zM3 14h7v7H3zM14 14h7v7h-7z',
 mail:'M3 5h18v14H3zM3 6l9 7 9-7',
 book:'M12 5c-3-2-6-2-10-1v15c4-1 7-1 10 1 3-2 6-2 10-1V4c-4-1-7-1-10 1v15',
 file:'M14 2H5v20h14V7zM14 2v6h5M8 12h8M8 16h6',
 pen:'m4 16 12-12 4 4L8 20l-5 1zM14 6l4 4',
 mic:'M9 5a3 3 0 0 1 6 0v7a3 3 0 0 1-6 0zM5 10v2a7 7 0 0 0 14 0v-2M12 19v3M8 22h8',
 plus:'M12 5v14M5 12h14', chevron:'m9 5 7 7-7 7', down:'m6 9 6 6 6-6',
 arrow:'M4 12h16m-6-6 6 6-6 6', back:'M20 12H4m6-6-6 6 6 6',
 check:'m5 12 4 4L19 6', close:'m6 6 12 12M6 18 18 6',
 sun:'M12 1v2M12 21v2M1 12h2M21 12h2M4 4l2 2M18 18l2 2M4 20l2-2M18 6l2-2M17 12a5 5 0 1 1-10 0 5 5 0 0 1 10 0',
 moon:'M21 13A9 9 0 0 1 11 3a9 9 0 1 0 10 10',
 settings:'M4 7h16M4 17h16M9 4v6M16 14v6',
 clock:'M12 8v5l3 2M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0',
 star:'m12 2 3 6 7 1-5 5 1 7-6-3-6 3 1-7-5-5 7-1z',
 archive:'M3 3h18v5H3zM5 8v13h14V8M9 12h6',
 upload:'M12 16V3m-5 5 5-5 5 5M4 15v6h16v-6',
 link:'m10 14 4-4M8 16l-2 2a4 4 0 0 1-5-5l5-5a4 4 0 0 1 5 0m2 5a4 4 0 0 0 5 0l5-5a4 4 0 0 0-5-5l-2 2',
 camera:'M3 7h4l2-3h6l2 3h4v14H3zM16 14a4 4 0 1 1-8 0 4 4 0 0 1 8 0',
 play:'m8 4 12 8-12 8z', pause:'M8 4v16M16 4v16',
 heart:'M12 21 3 12C-3 3 8-1 12 6 16-1 27 3 21 12z',
 bell:'M5 17h14l-2-3V8a5 5 0 0 0-10 0v6zM10 21h4',
 leaf:'M4 20C1 5 12 3 21 3c0 10-2 19-15 16M4 20 15 9',
 cloud:'M6 18a5 5 0 0 1-1-10 7 7 0 0 1 13-1 5 5 0 0 1 0 11M9 13l3 3 4-5',
 people:'M16 21v-3a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v3M12 6a3 3 0 1 1-6 0 3 3 0 0 1 6 0M18 3a3 3 0 0 1 0 6M22 21v-3a4 4 0 0 0-3-4',
 more:'M5 12h.01M12 12h.01M19 12h.01',
 layout:'M3 3h18v18H3zM8 3v18M8 8h13',
 layers:'m12 3 10 6-10 6L2 9zM2 14l10 6 10-6',
 undo:'M4 10h10a6 6 0 0 1 0 12M4 10l5-5M4 10l5 5',
 shield:'m12 2 9 4v6c0 5-9 10-9 10S3 17 3 12V6zM8 12l3 3 5-6',
 wifi:'M2 8a16 16 0 0 1 20 0M5 12a11 11 0 0 1 14 0M8 16a6 6 0 0 1 8 0M12 20h.01',
 list:'M8 6h13M8 12h13M8 18h13M3 6h.01M3 12h.01M3 18h.01',
 flag:'M4 22V3c6-5 10 5 16 0v11c-6 5-10-5-16 0',
 download:'M12 3v13m-5-5 5 5 5-5M4 16v5h16v-5',
 help:'M9 9a3 3 0 1 1 4 3v3M12 18h.01M22 12a10 10 0 1 1-20 0 10 10 0 0 1 20 0',
 spark:'m12 2 3 7 7 3-7 3-3 7-3-7-7-3 7-3z'
};
export const icon = (name, cls='') => `<svg class="icon ${cls}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.65" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="${paths[name]||paths.file}"/></svg>`;
export const button = (text, action, cls='', name='') => `<button class="btn ${cls}" data-action="${action}"${!text?` aria-label="${esc(action.replaceAll('-',' '))}" title="${esc(action.replaceAll('-',' '))}"`:''}>${name?icon(name):''}${text}</button>`;
export const avatar = (letters, tone='sage') => `<span class="avatar ${tone}">${letters}</span>`;
export const tag = (text, cls='') => `<span class="tag ${cls}">${text}</span>`;
export function sprig(){return `<svg class="sprig" viewBox="0 0 280 210" fill="none" aria-hidden="true"><path d="M62 187c52-12 75-53 120-134M120 145c-16-38-41-43-63-37 2 34 25 49 63 37ZM139 119c24-35 61-39 83-19-18 26-44 38-83 19ZM158 91c-29-12-34-45-15-63 28 13 34 31 15 63Z" fill="#97b88b" stroke="#385c42" stroke-width="2"/><path d="M180 59c-4-29 15-48 42-48 8 26-7 49-42 48Z" fill="#d3e4b9" stroke="#385c42" stroke-width="2"/><path d="M70 187c-33 1-42-22-38-42 28-2 43 11 38 42Z" fill="#d3e4b9" stroke="#385c42" stroke-width="2"/><path d="m119 145-39-25m61-1 58-15m-42-14-6-44" stroke="#385c42" stroke-width="1.5"/><circle cx="237" cy="151" r="17" fill="#efe5ae"/><path d="m234 144 6 13m-10-7 14 2" stroke="#827438"/><path d="M38 62h16M46 54v16" stroke="#65815b" stroke-width="2"/></svg>`;}
export const docs=[
 {title:'A little more room to think',type:'Notebook',meta:'12 pages · Edited just now',tone:'sage',route:'notebook',sub:'Notes, sketches & a few good ideas'},
 {title:'The quiet spaces project',type:'PDF document',meta:'8 pages · Opened 2 hours ago',tone:'lilac',route:'document',sub:'Research notes / September 2026'},
 {title:'Walks worth remembering',type:'Voice note',meta:'03:24 · Yesterday',tone:'butter',route:'capture',sub:'A thought from the long way home'},
 {title:'Small things, thoughtfully made',type:'Reading',meta:'Chapter 3 · Yesterday',tone:'rose',route:'document',sub:'An introduction to everyday design'},
 {title:'Field notes from the garden',type:'Notebook',meta:'6 pages · Tuesday',tone:'sage',route:'notebook',sub:'Observations & other little discoveries'},
 {title:'Weekend reading list',type:'Collection',meta:'5 items · Monday',tone:'lilac',route:'library',sub:'For when the world slows down'}
];
export function cover(d,mini=false){return `<div class="cover ${d.tone} ${mini?'small':''}"><span class="cover-label">${d.type}</span><strong>${d.title}</strong>${d.type==='Voice note'?wave():d.tone==='sage'?sprig():`<div class="cover-art"><i></i><i></i><i></i></div>`}<span class="cover-bottom">${d.sub}</span></div>`;}
export function wave(){return `<div class="wave" aria-label="Illustrative audio waveform">${Array.from({length:42},(_,i)=>`<i style="--h:${14+(Math.sin(i*1.8)+1)*17+(i%5)*4}px;--i:${i}"></i>`).join('')}</div>`;}
function section(title,action='View all',route='library'){return `<div class="section-head"><h2>${title}</h2><a href="#${route}">${action}${icon('chevron')}</a></div>`;}
export function home(){return `<div class="page home-page"><div class="greeting"><div><p class="soft">Saturday, September 12</p><h1>A little room for everything.</h1><p>Your ideas, conversations, and the things you’re coming back to.</p></div><span class="greeting-flower">${icon('spark')}</span></div><div class="home-feature"><a class="continue-card" href="#document"><div class="feature-copy"><span class="tag">${icon('clock')} Pick up where you left off</span><h2>The quiet spaces<br>project</h2><p>A collection of ideas for places that give us<br class="desktop-only"> a little more room to think.</p><span class="read-progress"><i></i></span><span class="soft">Page 4 of 8 <span>Halfway to a new perspective</span></span><span class="btn primary">Continue reading ${icon('arrow')}</span></div><div class="feature-paper"><div class="paper-eyebrow">Fieldwork / Vol. 01</div><h3>The art of<br>making room.</h3>${sprig()}<div class="paper-lines"></div><span>Small spaces. Meaningful possibilities.</span></div></a><aside class="quick-capture"><span class="small-icon">${icon('pen')}</span><h2>Catch a little thought.</h2><p>Before it wanders off.</p><textarea id="quick-note" aria-label="Quick thought" placeholder="What’s on your mind?"></textarea><div class="capture-actions">${button('Save thought','save-thought','primary')}${button('','voice','icon-btn','mic')}</div><span class="soft">Only a beginning. That’s enough.</span></aside></div>${section('Make yourself at home','Open library')}<div class="document-grid">${docs.slice(0,4).map(d=>`<a class="doc-card" href="#${d.route}">${cover(d)}<div class="doc-meta"><strong>${d.title}</strong>${icon('more')}<span>${d.meta}</span></div></a>`).join('')}</div><div class="home-bottom"><div>${section('A few useful shortcuts','Explore journeys','journeys')}<div class="shortcut-row">${[['camera','Scan a page','scan'],['upload','Bring a file','import'],['people','Share something','sharing']].map(([i,t,j])=>`<a href="#journey/${j}">${icon(i)}<span>${t}</span>${icon('chevron')}</a>`).join('')}</div></div><div class="today-note"><span class="tiny-sprout">${icon('leaf')}</span><div><strong>A quieter kind of workspace.</strong><p>Everything has a place. Nothing needs your attention all at once.</p></div></div></div></div>`;}
export function library(query='',kind='All'){const filtered=docs.filter(d=>(kind==='All'||d.type===kind)&&`${d.title} ${d.type}`.toLowerCase().includes(query.toLowerCase()));return `<div class="page"><div class="page-heading"><div><h1>Your library</h1><p>A good place to keep a good thought.</p></div>${button('Add something','import','primary','plus')}</div><div class="collection-row">${[['sage','Everyday ideas','12 items'],['lilac','Quiet spaces','8 items'],['butter','For a slow Sunday','5 items']].map(([c,t,n])=>`<button class="collection ${c}" data-action="collection">${icon('book')}<strong>${t}</strong><span>${n}</span>${icon('chevron')}</button>`).join('')}</div><div class="library-toolbar"><div class="chips">${['All','Notebook','PDF document','Voice note','Reading'].map(k=>`<button data-kind="${k}" class="chip ${k===kind?'active':''}">${k==='All'?'Everything':k}</button>`).join('')}</div><label class="inline-search">${icon('search')}<input id="library-search" aria-label="Search library" placeholder="Find something…" value="${esc(query)}"></label></div><div class="document-grid library-grid">${filtered.map(d=>`<a class="doc-card" href="#${d.route}">${cover(d)}<div class="doc-meta"><strong>${d.title}</strong>${icon('more')}<span>${d.meta}</span></div></a>`).join('')||`<div class="empty-panel">${icon('search')}<h2>No matching pages, yet.</h2><p>Try a title, a thought, or a different filter.</p>${button('Clear search','clear-search')}</div>`}</div></div>`;}
const people=[['AM','Amelia Morgan','The quiet spaces project','A few thoughts on the first draft. What do you think?','10:42','sage'],['JL','Jamie Lee','Something for your reading list','This reminded me of our conversation yesterday.','09:18','lilac'],['NS','Nora Shah','Saturday, a little slower','The notes from our walk are ready.','Yesterday','butter'],['ET','Ellis Tan','Re: A place for small ideas','Yes! Let’s keep the original sketches too.','Yesterday','rose']];
export function inbox({step=0,compose=false,selected=0}={}){const p=people[selected]||people[0];return `<div class="inbox-layout"><aside class="mail-list"><div class="mail-list-head"><h1>Inbox <span>4</span></h1>${button('','compose','icon-btn','pen')}</div><div class="mail-tabs"><button class="active" data-action="all-mail">All mail</button><button data-action="unread">Unread <span>2</span></button></div>${people.map((x,i)=>`<button class="mail-item ${i===selected?'selected':''}" data-thread="${i}">${avatar(x[0],x[5])}<span><strong>${x[1]}<time>${x[4]}</time></strong><b>${x[2]}</b><small>${x[3]}</small>${i===0?tag('1 attachment'):''}</span>${i<2?'<i class="unread-dot"></i>':''}</button>`).join('')}<div class="mail-list-foot">${icon('leaf')} Just the conversations that matter.</div></aside><article class="conversation"><div class="conversation-tools"><span>${icon('mail')} Personal inbox</span><div>${button('','archive','icon-btn','archive')}${button('','reminder','icon-btn','clock')}${button('','share','icon-btn','more')}</div></div><div class="conversation-body"><div class="conversation-title"><h2>${compose?'A thought worth sharing':p[2]}</h2>${button('','star','icon-btn','star')}</div><div class="sender">${avatar(p[0],p[5])}<div><strong>${p[1]}</strong><small>to you <span>Today at ${p[4]}</span></small></div>${icon('more')}</div><div class="letter"><p>Hey Alex,</p><p>I’ve put together a few ideas for the quiet spaces project. The attached notes bring together our sketches, the little observations, and a possible direction.</p><p>Could you add your thoughts to the second section? There’s something lovely in the idea of making a place feel a little less hurried.</p><p>No rush. Good things need a little room.</p><p>Amelia</p></div><button class="attachment" data-action="open-document"><span class="file-icon lilac">${icon('file')}</span><span><strong>Quiet spaces — working notes.pdf</strong><small>8 pages · 2.4 MB</small></span><span class="attachment-open">Open & annotate ${icon('arrow')}</span></button><div class="reply-box"><div class="reply-heading">${icon('undo')} Reply to ${p[1]}<span>Draft</span></div><textarea id="reply-text" aria-label="Reply message" placeholder="A little thought in reply…">${compose||step>2?'I love this direction. I’ve added a few thoughts to the notes — especially around light, texture, and leaving space to pause.':''}</textarea>${step>2?`<div class="reviewed-file">${icon('file')} Quiet spaces — reviewed.pdf ${tag('Original kept')}</div>`:''}<div class="reply-actions"><div>${button('','attach','icon-btn','plus')}<span class="format-tools">Aa &nbsp; B &nbsp; <em>I</em></span></div>${button('Send reply','send','primary','arrow')}</div></div></div></article></div>`;}
export function documentScreen({step=0,mode='read'}={}){return `<div class="reader-layout"><div class="reader-toolbar"><a href="#library" class="reader-back">${icon('back')} Library</a><span>Quiet spaces — working notes.pdf</span><div>${button('Share','share','','people')}${button('Save copy','save-copy','primary','download')}</div></div><div class="reader-body"><aside class="page-outline"><span>Contents</span>${['A little introduction','Making room','Light, texture & time','Notes to return to'].map((t,i)=>`<button class="outline-item ${i===1?'active':''}" data-action="page"><span>0${i+1}</span>${t}</button>`).join('')}<div class="page-thumbnails">${[1,2,3].map(n=>`<button data-action="page" class="page-thumb ${n===2?'selected':''}"><div><span>Quiet spaces</span><i></i><i></i><i></i></div>${n}</button>`).join('')}</div></aside><div class="reading-canvas"><div class="annotation-tools">${['pen','file','sun','undo','search'].map((i,n)=>button('',n===0?'annotate':'tool',`icon-btn ${n===0&&mode!=='read'?'selected':''}`,i)).join('')}<span class="tool-divider"></span><button class="color-dot yellow" data-action="highlight" aria-label="Butter highlight"></button><button class="color-dot green" data-action="highlight" aria-label="Sage highlight"></button><button class="color-dot purple" data-action="highlight" aria-label="Lavender highlight"></button></div><article class="document-paper"><div class="paper-eyebrow">The quiet spaces project <span>02 / Observations</span></div><h1>Room to pause.</h1><p class="paper-lead">What if the most useful thing a place could give us was a little less?</p><div class="document-illustration">${sprig()}<span>Let the ordinary<br>be enough.</span></div><h3>Making room for the everyday</h3><p>We tend to notice what fills a room. The chair by the window, the books on the table, the warm light that finds its way in just before evening.</p><p><mark>The spaces between these things matter just as much.</mark> A place to put down a thought. A moment without a next step. A small invitation to simply be here.</p>${mode==='forms'?'<label class="paper-form">Your observation<input placeholder="What did you notice?" value="The light changes everything."></label>':`<p ${mode==='edit'?'contenteditable="true" role="textbox" aria-label="Edit document paragraph"':''}>${step>1?'Let the design make room for people, rather than asking people to make room for the design.':'Our first sketches start here: with fewer decisions, softer edges, and a little more room to breathe.'}</p>`}<div class="paper-footer"><span>Fieldwork, September 2026</span><span>4</span></div></article><div class="page-position">${button('','page-back','icon-btn','back')}<span>Page 4 <small>of 8</small></span>${button('','page-next','icon-btn','arrow')}<span class="zoom-label">100%</span></div></div><aside class="margin-notes"><span class="soft">In the margins</span><div class="margin-note">${avatar('AM')}<strong>Amelia</strong><p>Could this be our starting point? Less noise. More space.</p><small>On this page · 10:42</small></div><button class="add-note" data-action="comment">${icon('plus')} Leave a thought</button><div class="custody-note">${icon('shield')}<p>Your original stays as it is. Every edit gets its own copy.</p></div></aside></div></div>`;}
export function notebook(){return `<div class="notebook-layout"><div class="reader-toolbar"><a href="#library">${icon('back')} Notebooks</a><span>A little more room to think</span><div>${tag('Saved in this preview')}${button('Share','share','','people')}</div></div><div class="notebook-workspace"><aside class="notebook-pages"><h3>Pages ${button('','add-page','icon-btn','plus')}</h3>${['A little beginning','Things I noticed','A gentler direction'].map((t,i)=>`<button data-action="note-page" class="${i===0?'active':''}"><span>${i+1}</span>${t}</button>`).join('')}</aside><article class="notebook-paper"><span class="hand-date">September 12, a Saturday</span><h1>A little beginning.</h1><p contenteditable="true" role="textbox" aria-label="Notebook text">Some ideas arrive quietly.<br>On a walk. Between conversations.<br>Just before you fall asleep.</p><div class="handwritten">Make a little room for them.</div><div class="sketch-row"><div class="sketch-circle">notice<br>↓<br>collect<br>↓<br>make</div>${sprig()}</div><div class="note-checks"><label><input type="checkbox" checked> Keep the first thought, even if it’s messy.</label><label><input type="checkbox"> Go for a walk without an agenda.</label><label><input type="checkbox"> Come back and see what stayed.</label></div><div class="notebook-sticker">a work<br>in progress ${icon('heart')}</div></article></div><div class="ink-dock">${['pen','file','sun','undo'].map(i=>button('','ink','icon-btn',i)).join('')}<span class="tool-divider"></span>${['green','yellow','purple','black'].map(c=>`<button class="color-dot ${c}" data-action="ink-color" aria-label="${c} ink"></button>`).join('')}<span class="tool-divider"></span>${button('Done','note-done','primary','check')}</div></div>`;}
export function capture(mode='voice',step=0){return `<div class="page capture-page"><div class="page-heading"><div><h1>${mode==='scan'?'A page, a new possibility.':mode==='import'?'Bring something along.':'Catch the thought.'}</h1><p>${mode==='scan'?'From the world in front of you to a place you can return to.':mode==='import'?'Notes, documents, images. There’s room for all of them.':'Some ideas sound better out loud.'}</p></div>${tag('Sample capture')}</div><div class="capture-choice">${[['mic','Voice','voice'],['camera','Scan','scan'],['upload','Import','import']].map(([i,t,r])=>`<a class="${r===mode?'active':''}" href="#journey/${r}">${icon(i)}${t}</a>`).join('')}</div>${mode==='voice'?`<div class="voice-studio"><div class="record-orb">${icon('mic')}</div><h2>${step===0?'A thought from the long way home':'Walks worth remembering'}</h2><p>${step===0?'Your next idea can start with a single sentence.':'03:24 · Recorded in the design preview'}</p>${wave()}<div class="record-controls">${button('','play-audio','round-btn','play')}${button(step===0?'Start sample recording':'Review transcript','record','primary','mic')}</div></div><div class="transcript"><div class="section-head"><h2>The thought, in words.</h2>${tag('Sample transcript')}</div><p>“There’s something about taking the long way home. You notice the garden you’ve walked past a hundred times. The way the light falls on that one wall. Maybe the quiet spaces project should start there. With the things we nearly miss.”</p><div class="chips">${button('Make a note','note-done','','pen')}${button('Keep with a document','attach','','file')}</div></div>`:mode==='scan'?`<div class="scan-studio"><div class="scan-frame"><div class="scan-paper"><span>Field notes</span><h2>A slower<br>kind of day.</h2><p>Light through the window.<br>A thought worth keeping.<br>Room for what comes next.</p>${sprig()}</div><i class="scan-corner a"></i><i class="scan-corner b"></i><i class="scan-corner c"></i><i class="scan-corner d"></i></div><div class="scan-controls"><span>Sample page ${step>1?'captured':'in frame'}</span>${button('','capture-page','shutter','camera')}<span>Auto crop ${icon('check')}</span></div></div>`:`<div class="import-zone">${icon('upload')}<h2>A fresh place for your files.</h2><p>Drop a PDF, notebook, image, or a few words.</p>${button('Choose a sample file','sample-import','primary','plus')}<small>Design preview · no personal files are uploaded</small></div><div class="import-recent"><h3>Or start with a little inspiration</h3>${docs.slice(0,2).map(d=>`<button class="attachment" data-action="sample-import"><span class="file-icon ${d.tone}">${icon('file')}</span><span><strong>${d.title}</strong><small>${d.type}</small></span>${icon('plus')}</button>`).join('')}</div>`}</div>`;}
export function settings(){return `<div class="page settings-page"><div class="page-heading"><div><h1>Make it feel like you.</h1><p>A few thoughtful choices. A more comfortable workspace.</p></div></div><div class="settings-section"><div><h2>Appearance</h2><p>A different light for a different moment.</p></div><div class="theme-options">${['light','dark'].map(t=>`<button class="theme-choice ${t}" data-theme="${t}"><div class="theme-mini"><i></i><span><b></b><em></em><em></em></span></div><strong>${t==='light'?'Morning light':'After hours'}</strong><small>${t==='light'?'Fresh paper & soft sage':'Deep forest & quiet contrast'}</small></button>`).join('')}</div></div><div class="settings-section"><div><h2>The little details</h2><p>Settle into your own pace.</p></div><div class="setting-rows"><label><span><strong>Gentle motion</strong><small>Small transitions that help you find your place.</small></span><input type="checkbox" role="switch" id="motion-toggle" checked></label><label><span><strong>A little more breathing room</strong><small>Larger text for longer reading sessions.</small></span><input type="checkbox" role="switch" id="text-toggle"></label><label><span><strong>Quiet reminders</strong><small>Only the things you asked to come back to.</small></span><input type="checkbox" role="switch" checked></label></div></div><div class="settings-section"><div><h2>Your space</h2><p>Sample account for this design edition.</p></div><div class="profile-card">${avatar('AL')}<div><strong>Alex Lane</strong><small>Personal workspace</small></div>${tag('Design preview')}</div></div></div>`;}
export function utility(id,step=0){const config={
 accounts:['Bring your worlds together.','Choose the accounts you want to feel at home here.','Connect sample account','people'],
 sharing:['Good things are better shared.','A little clarity about who can see what.','Share in preview','people'],
 reminders:['Come back at the right moment.','Leave a small note for your future self.','Set sample reminder','clock'],
 backup:['Your work, safely kept.','Keep a copy of the things you don’t want to lose.','Preview backup','shield'],
 offline:['A little less connected. Still here.','Your saved pages stay close, even when the internet doesn’t.','Keep working','wifi'],
 conflicts:['Two thoughts. Room for both.','Choose how these versions come back together.','Keep both versions','layers'],
 'delete-account':['Leave things in good order.','Keep what matters before you close this chapter.','Review sample deletion','shield'],
 help:['Let’s find your way back.','A little guidance for the moments that don’t go to plan.','Open recovery guide','help'],
 release:['A place that comes with you.','The same quiet workspace, wherever you open it.','Preview installation','download'],
 export:['A copy, ready for what’s next.','Choose a format. Your original stays right here.','Prepare sample export','download'],
 pages:['Put things in a better order.','Move, select, and collect the pages that belong together.','Save arranged copy','layers']
 }[id]||['A little more room.','Everything you need, right where you left it.','Continue preview','leaf'];
const [title,desc,cta,i]=config;
let content='';
if(id==='accounts')content=`<div class="provider-cards">${[['G','Gmail','Conversations, kept in context.','sage'],['S','Slack','A place for the team’s thoughts.','lilac'],['D','Google Drive','Your documents, closer at hand.','butter']].map(([a,t,d,c])=>`<button class="provider-card" data-action="connect">${avatar(a,c)}<strong>${t}</strong><p>${d}</p><span>${step>2?'Connected in preview':'Choose account'} ${icon('arrow')}</span></button>`).join('')}</div><div class="scope-note">${icon('shield')} You choose what comes in. Nothing is accessed in this prototype.</div>`;
else if(id==='sharing')content=`<div class="share-panel"><div class="attachment"><span class="file-icon sage">${icon('book')}</span><span><strong>The quiet spaces project</strong><small>A thoughtful place to start</small></span></div><label class="invite-field">Invite someone<input placeholder="Name or email address" aria-label="Invite recipient"></label><div class="person-row">${avatar('AL')}<span>Alex Lane <small>You</small></span><b>Owner</b></div><div class="person-row">${avatar('AM','lilac')}<span>Amelia Morgan</span><select aria-label="Amelia access"><option>Can comment</option><option>Can view</option><option>Can edit</option></select></div><div class="access-note">${icon('shield')} Only invited people can open this document.</div></div>`;
else if(id==='reminders')content=`<div class="reminder-options">${[['Later today','Today, 6:00 PM','sun'],['Tomorrow morning','Sunday, 9:00 AM','leaf'],['Next week','Monday, 9:00 AM','clock']].map(([t,d,x],n)=>`<label><input type="radio" name="reminder" ${n===1?'checked':''}>${icon(x)}<span><strong>${t}</strong><small>${d}</small></span></label>`).join('')}<label><span>Pick your own moment</span><input type="datetime-local" aria-label="Custom reminder time"></label></div>`;
else if(id==='conflicts')content=`<div class="version-grid">${[['Your version','Today, 10:32','Start with the places we return to.'],['Amelia’s version','Today, 10:34','Start with the little things we nearly miss.']].map(([t,d,p])=>`<article><span>${t}</span><small>${d}</small><h3>A little beginning.</h3><p>${p}</p><label><input type="checkbox" checked> Keep this version</label></article>`).join('')}</div>`;
else if(id==='pages')content=`<div class="organize-pages">${[1,2,3,4].map((n)=>`<label class="organize-page"><input type="checkbox" checked><div class="mini-paper"><small>Quiet spaces / ${n}</small><h3>${['A beginning.','Room to pause.','A gentler light.','What stays.'][n-1]}</h3><div class="paper-lines"></div></div><span>Page ${n}</span></label>`).join('')}</div>`;
else if(id==='export')content=`<div class="export-options"><label>File name<input value="Quiet spaces — reviewed"></label><label>Format<select><option>PDF document</option><option>Plain text</option><option>PNG image</option></select></label><label>Pages<select><option>All 8 pages</option><option>Current page</option></select></label><label class="check-label"><input type="checkbox" checked> Include my annotations</label><div class="access-note">${icon('file')} Original document will be kept.</div></div>`;
else if(id==='delete-account')content=`<div class="leave-panel"><div class="notice gentle">${icon('book')} 6 documents and 2 notebooks belong to this sample workspace.</div><label class="check-label"><input type="checkbox"> I have kept a copy of my work.</label><label>Type “leave” to preview confirmation<input placeholder="leave" aria-label="Deletion confirmation"></label><p>No real account or files can be deleted here.</p></div>`;
else if(id==='help')content=`<div class="help-options">${[['Find a missing document','Your library, recent work, and recovered drafts.'],['Pick up after an interruption','Get back to the page you were working on.'],['Keep a copy of your work','A little reassurance, one backup at a time.']].map(([t,d])=>`<button data-action="help-topic"><strong>${t}</strong><small>${d}</small>${icon('chevron')}</button>`).join('')}</div>`;
else if(id==='release')content=`<div class="install-art"><div class="app-tile">j<span>jty</span></div><h2>JJTY</h2><p>Your little room for everything.</p>${tag('Design preview · native build not included')}</div>`;
else content=`<div class="recovery-panel"><div class="recovery-icon">${icon(i)}</div><h2>${id==='offline'?'6 documents, right here.':'Everything worth keeping.'}</h2><p>${id==='offline'?'Read, write, and make changes to your sample pages.':'Documents, annotations, and the little thoughts in between.'}</p>${docs.slice(0,3).map(d=>`<div class="recovery-row">${icon('file')}<span>${d.title}</span>${icon('check')}</div>`).join('')}<div class="progress-track"><i style="width:${step>1?'100':'72'}%"></i></div><small>${step>1?'Sample preparation complete':'Preview of preparation progress'}</small></div>`;
return `<div class="page utility-page"><div class="utility-heading"><span class="utility-symbol">${icon(i)}</span><h1>${title}</h1><p>${desc}</p></div>${content}<div class="utility-footer">${button('Back to my space','go-home')}${button(cta,'utility-next','primary','arrow')}</div></div>`;}
export function journeySurface(j,step){const id=j.id;
if(id==='email-pdf-reply')return step===1||step===2?documentScreen({step,mode:'edit'}):inbox({step,compose:step>2});
if(id==='inbox'||id==='compose')return inbox({step,compose:id==='compose'});
if(id==='home')return home();if(id==='search')return library(step>0?'quiet':'');
if(['notebook','handwriting'].includes(id))return notebook();
if(['scan','voice','import'].includes(id))return capture(id,step);
if(['pdf-read','epub','pdf-edit','annotate','forms'].includes(id))return documentScreen({step,mode:id==='forms'?'forms':id==='pdf-edit'?'edit':id==='annotate'?'annotate':'read'});
if(id==='settings')return settings();return utility(id,step);
}
