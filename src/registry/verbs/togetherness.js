import { designedVerb } from "../define.js";
export default designedVerb({
  id: "togetherness",
  spokenForms: ["read this together", "join me here"],
  description: "Let two people stay present in the same document.",
  roomDescription: "Togetherness will let two people read one document with live places and explicit control over what is shared.",
  testReference: "test/registry.test.mjs",
});
