# Direct visual page organization

Continue the founder's own-UI requirement. Reuse the existing immutable reviewed PDF snapshot and verified reorder/extract/rotate handlers, retaining all preflight restrictions, generation guards, rollback and undo. Add a visual page overview to that review with page selection, move earlier/later and extract selected controls; each action submits the existing verified operation. The Organize workspace exposes an explicit JETT Organize pages entry, with original-copy scope stated. Bento remains secondary.

Do not stage a cosmetic reorder: every move regenerates and verifies the real PDF before updating the preview. Clear selection on a new snapshot; disable every visual mutation during pending work. No new engine, source mutation or duplicate renderer. Responsive grid uses the already rendered review canvases. Keep full-size inspection available. Keyboard controls remain native buttons/checkboxes with page labels.

Verify visual move, undo, multi-page extraction and downloaded page order with independent PDF parsing. Existing restricted-PDF gates must disable visual actions. Run focused review regressions then full suite, inspect desktop/phone renders and independent review. Full parity and durable library publication remain separate unmet gates.
