# Order P4-GLASS-ENGINE

> Codex build agent for jt (lowercase brand). Order P4-GLASS-ENGINE. This directory is an empty git repo — build jt-glass from scratch (skeleton on main, work on wo/p4-glass). Read /Users/apple/Documents/Codex/2026-08-10/get/outputs/JT_CAPABILITY_MAP_V2_2026-08-13.md belt 7 (the design) and /Users/apple/projects/jt-web/src (the engine you are making transparent — do NOT modify jt-web; build standalone + an integration patch). BUILD the glass engine v0 — transparency as a feature, for developers AND users: (1) an event-tap contract: a tiny module (src/tap.ts) defining a typed event stream {transcriptEvent, segmentationDecision, matchScores per block/span, intentResult incl. ambiguities+thresholds, actCommitted, recordWritten, latencyMark} — designed so jt-web emits these with a ~10-line hook; (2) the glass panels (framework-free TS components): live transcript with segmentation coloring; matcher heat-strip (per-block scores, span pin); decision log ('asked because two passages scored within 0.04'); record inspector with schema-validation state; latency meters vs budgets; (3) the REPLAY LAB: load a session fixture (JSONL of tap events), scrub through it, diff against an expected outcome file; record new fixtures from a live stream; (4) a demo page wiring the panels to a bundled fixture session (no jt-web dependency at runtime; zero network); (5) patches/jt-web-tap.patch — git diff against /Users/apple/projects/jt-web main adding the emit hook + a dev-only #/glass route hosting the panels; verify with git apply --check; do NOT commit into jt-web. Tests: tap contract validation, replay determinism (same fixture → same panel state), diff detection on a mutated fixture. Plain human words in all panel copy; banned words: QR anything, Margin as name, locus/settle/receipt as UI nouns (schema wire names may appear inside raw record JSON views — that is data, fine), sea/drops/shells as product nouns. Try to commit with orders/P4.md + receipts/P4.md (verbatim outputs); if git blocked leave tree ready and say so. Print final summary.

