# P4-GLASS-ENGINE receipt

Date: 2026-08-13

Workspace: `/Users/apple/projects/jt-glass`

Requested branch: `wo/p4-glass`

Actual Git state: unborn `main`; `.git` is read-only in this environment.

jt-web authority at start: `/Users/apple/projects/jt-web` `main` at
`9d1611813854249d65353995474a7e31880c0447`. Final authority after the P1 merge:
`7da8e677ddd7851b3f7f2cd9d35bf399a364cb7f`; the final patch targets this SHA.

## Automated tests

Command: `npm test`

Verbatim output:

```text
> @jt/glass@0.0.0 test
> node --test

✔ the build produces a self-contained browser demo (108.1705ms)
✔ panel markup escapes session data and explains each visible area (0.9515ms)
✔ the same fixture always produces the same panel state (9.040583ms)
✔ a changed match score is reported at its exact outcome path (1.759583ms)
✔ a recorder captures only the live events between start and stop (0.328541ms)
✔ a malformed JSONL line names the line and contract problem (0.311459ms)
✔ a recorder can update a live replay as each event arrives (0.129125ms)
✔ the tap contract accepts a complete transcript event (0.966ms)
✔ the tap contract explains malformed match spans (0.230833ms)
✔ a tap publishes in order and records portable JSONL (0.189125ms)
✔ every event kind crosses the JSON boundary (1.822542ms)
ℹ tests 11
ℹ suites 0
ℹ pass 11
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 232.132041
```

Exit: `0`.

## Final ordinary-open correction

Final code review identified that jt-web's `openDocument()` returns `null` after
a successful ordinary open when no saved reading position exists. The
integration now waits for both ordinary open paths to complete and emits
`handled` without treating that optional position as success evidence. The
`requirePosition` return path still uses the nullable position result.

The corrected patch applied with `--whitespace=error-all` to a fresh archive of
jt-web `7da8e677ddd7851b3f7f2cd9d35bf399a364cb7f`. Its isolated production build
transformed 186 modules and ended with this verbatim result:

```text
✓ built in 248ms
```

The final combined gate output was:

```text
## main...origin/main
7da8e677ddd7851b3f7f2cd9d35bf399a364cb7f

> @jt/glass@0.0.0 check
> npm test && npm run build

> @jt/glass@0.0.0 test
> node --test

✔ the build produces a self-contained browser demo (117.889208ms)
✔ panel markup escapes session data and explains each visible area (0.752167ms)
✔ the exported mount heading escapes a caller-provided title (0.0875ms)
✔ the same fixture always produces the same panel state (7.984875ms)
✔ a changed match score is reported at its exact outcome path (1.416209ms)
✔ a recorder captures only the live events between start and stop (0.58175ms)
✔ a malformed JSONL line names the line and contract problem (0.279458ms)
✔ a recorder can update a live replay as each event arrives (0.112041ms)
✔ new interim words replace the previous changing hypothesis (0.128667ms)
✔ replay rejects mixed sessions even when called from plain JavaScript (0.114375ms)
✔ the tap contract accepts a complete transcript event (1.22375ms)
✔ the tap contract explains malformed match spans (0.313333ms)
✔ a tap publishes in order and records portable JSONL (0.758917ms)
✔ every event kind crosses the JSON boundary (0.591625ms)
✔ event times must include a canonical ISO time (0.104625ms)
ℹ tests 15
ℹ suites 0
ℹ pass 15
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 263.728542

> @jt/glass@0.0.0 build
> node --no-warnings scripts/build.mjs

built /Users/apple/projects/jt-glass/dist
panel copy scan: 0 banned UI terms
demo network scan: 0 network calls or remote URLs
workspace temp scan: clean
workspace diff check: clean
```

Exit: `0`.

## Final command-outcome correction

The follow-up review caught one remaining truthfulness issue: non-highlight
commands were described before execution. The contract now includes the plain
`handled` result, the panel renders it as `Handled`, and the jt-web patch emits
undo, range, open, return, send, and show outcomes only from their actual success,
question, or failure branches.

Final command:

```sh
git -C /Users/apple/projects/jt-web apply --check --whitespace=error-all /Users/apple/projects/jt-glass/patches/jt-web-tap.patch && npm run check && git diff --check
```

Verbatim output:

```text
> @jt/glass@0.0.0 check
> npm test && npm run build

> @jt/glass@0.0.0 test
> node --test

✔ the build produces a self-contained browser demo (106.705917ms)
✔ panel markup escapes session data and explains each visible area (0.696958ms)
✔ the exported mount heading escapes a caller-provided title (0.151416ms)
✔ the same fixture always produces the same panel state (5.257833ms)
✔ a changed match score is reported at its exact outcome path (2.015166ms)
✔ a recorder captures only the live events between start and stop (0.284917ms)
✔ a malformed JSONL line names the line and contract problem (0.235083ms)
✔ a recorder can update a live replay as each event arrives (0.096375ms)
✔ new interim words replace the previous changing hypothesis (0.1275ms)
✔ replay rejects mixed sessions even when called from plain JavaScript (0.12375ms)
✔ the tap contract accepts a complete transcript event (0.939916ms)
✔ the tap contract explains malformed match spans (1.273709ms)
✔ a tap publishes in order and records portable JSONL (1.104667ms)
✔ every event kind crosses the JSON boundary (0.3835ms)
✔ event times must include a canonical ISO time (0.0885ms)
ℹ tests 15
ℹ suites 0
ℹ pass 15
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 246.684416

> @jt/glass@0.0.0 build
> node --no-warnings scripts/build.mjs

built /Users/apple/projects/jt-glass/dist
```

Exit: `0`.

Final copy, network, authority, and workspace status:

```text
panel copy scan: 0 banned UI terms
demo network scan: 0 network calls or remote URLs
## main...origin/main
9d1611813854249d65353995474a7e31880c0447
9d1611813854249d65353995474a7e31880c0447
## No commits yet on main
?? .gitignore
?? README.md
?? demo.ts
?? docs/
?? fixtures/
?? index.html
?? orders/
?? package.json
?? patches/
?? receipts/
?? scripts/
?? src/
?? test/
```

## Standalone build

Command: `npm run build`

Verbatim output:

```text
> @jt/glass@0.0.0 build
> node --no-warnings scripts/build.mjs

built /Users/apple/projects/jt-glass/dist
```

Exit: `0`.

## Strict patch check

Command:
`git -C /Users/apple/projects/jt-web apply --check --whitespace=error-all /Users/apple/projects/jt-glass/patches/jt-web-tap.patch`

Verbatim output:

```text
(no output)
```

Exit: `0`.

## Applied-copy jt-web production build

The patch was applied to a fresh `git archive main` copy. Existing jt-web Vite
and PDF packages and this jt-glass working tree were linked into that disposable
copy only. jt-web itself was not changed.

Command:
`node /Users/apple/projects/jt-web/node_modules/vite/bin/vite.js build --configLoader runner 2>&1 | tail -12`

Verbatim output:

```text
dist/assets/jt_core-CtLh492r.js                           5.71 kB │ gzip:   2.12 kB
dist/assets/pdf-wuz5H9dQ.js                             427.30 kB │ gzip: 127.39 kB
dist/assets/index-DGRPlW-d.js                           518.87 kB │ gzip: 156.96 kB

[INEFFECTIVE_DYNAMIC_IMPORT] vendor/jt-speech/math/spokenMathToLatex.js is dynamically imported by src/shell.js but also statically imported by src/math.js, vendor/jt-speech/index.js, dynamic import will not move module into another chunk.

[plugin builtin:vite-reporter]
(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Use build.rolldownOptions.output.codeSplitting to improve chunking: https://rolldown.rs/reference/OutputOptions/codeSplitting
- Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
✓ built in 236ms
```

Exit: `0`. The two warnings are pre-existing jt-web bundling warnings and do not
stop the build.

## Copy boundary

Command:
`if rg -n -i '\b(qr|locus|settle|receipt|sea|drops|shells)\b|\bMargin\b' src/panels.ts src/replay-lab.ts src/index.ts demo.ts index.html; then exit 1; else echo 'panel copy scan: 0 banned UI terms'; fi`

Verbatim output:

```text
panel copy scan: 0 banned UI terms
```

Exit: `0`.

## Zero-network demo boundary

Command:
`if rg -n 'https?://|fetch\(|XMLHttpRequest|WebSocket|EventSource' index.html demo.ts src fixtures; then exit 1; else echo 'demo network scan: 0 network calls or remote URLs'; fi`

Verbatim output:

```text
demo network scan: 0 network calls or remote URLs
```

Exit: `0`.

## jt-web remained untouched

Commands:

```sh
git -C /Users/apple/projects/jt-web status --short --branch
git -C /Users/apple/projects/jt-web rev-parse HEAD
git -C /Users/apple/projects/jt-web rev-parse main
```

Verbatim output:

```text
## main...origin/main
9d1611813854249d65353995474a7e31880c0447
9d1611813854249d65353995474a7e31880c0447
```

Exit: `0`.

## Visual verification boundary

The static build completed. A local server attempt failed verbatim with:

```text
Error: listen EPERM: operation not permitted 127.0.0.1:4177
```

The in-app browser then rejected direct `file:` navigation under its URL policy.
No visual browser pass is claimed. Rendering safety and visible panel labels are
covered by `test/panels.test.ts`; responsive CSS exists at 900 px and 560 px.

## Git boundary

Initial requested skeleton commit command failed verbatim with:

```text
fatal: Unable to create '/Users/apple/projects/jt-glass/.git/index.lock': Operation not permitted
```

The final working-tree state before the last commit attempt was:

```text
## No commits yet on main
?? .gitignore
?? README.md
?? demo.ts
?? docs/
?? fixtures/
?? index.html
?? orders/
?? package.json
?? patches/
?? receipts/
?? scripts/
?? src/
?? test/
fatal: Needed a single revision
main
```

All source and evidence are left ready to stage. The exact history to create when
Git metadata becomes writable is: skeleton commit on `main`, branch
`wo/p4-glass`, then implementation commit including `orders/P4.md` and this file.

Final commit attempt:

```sh
git diff --check && git add .gitignore README.md demo.ts docs fixtures index.html orders package.json patches receipts scripts src test && git commit -m 'feat: build jt glass engine v0'
```

Verbatim output:

```text
fatal: Unable to create '/Users/apple/projects/jt-glass/.git/index.lock': Operation not permitted
```

Exit: `128`.

## Final post-review verification

An independent review found six important truthfulness gaps. They were fixed:
the host now reports the real target-choice result, all durable record writes are
tapped, undo/return acts are tapped, interim words replace stale hypotheses,
every document block receives one score strip, span units are explicit, restored
events are validated and kept to one session, and mount titles are escaped.

Command: `npm run check`

Verbatim output:

```text
> @jt/glass@0.0.0 check
> npm test && npm run build

> @jt/glass@0.0.0 test
> node --test

✔ the build produces a self-contained browser demo (114.901667ms)
✔ panel markup escapes session data and explains each visible area (0.9335ms)
✔ the exported mount heading escapes a caller-provided title (0.135542ms)
✔ the same fixture always produces the same panel state (18.471208ms)
✔ a changed match score is reported at its exact outcome path (1.856583ms)
✔ a recorder captures only the live events between start and stop (0.345458ms)
✔ a malformed JSONL line names the line and contract problem (0.394833ms)
✔ a recorder can update a live replay as each event arrives (0.169792ms)
✔ new interim words replace the previous changing hypothesis (0.144167ms)
✔ replay rejects mixed sessions even when called from plain JavaScript (0.112792ms)
✔ the tap contract accepts a complete transcript event (0.946917ms)
✔ the tap contract explains malformed match spans (0.281709ms)
✔ a tap publishes in order and records portable JSONL (0.734292ms)
✔ every event kind crosses the JSON boundary (0.335167ms)
✔ event times must include a canonical ISO time (0.081083ms)
ℹ tests 15
ℹ suites 0
ℹ pass 15
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 261.722583

> @jt/glass@0.0.0 build
> node --no-warnings scripts/build.mjs

built /Users/apple/projects/jt-glass/dist
```

Exit: `0`.

The revised patch was then applied to another fresh jt-web `main` archive and
built. Final lines, verbatim:

```text
dist/assets/jt_core-CtLh492r.js                           5.71 kB │ gzip:   2.12 kB
dist/assets/pdf-Danwn5pA.js                             427.30 kB │ gzip: 127.39 kB
dist/assets/index-BjIS-_VZ.js                           520.96 kB │ gzip: 157.51 kB

[INEFFECTIVE_DYNAMIC_IMPORT] vendor/jt-speech/math/spokenMathToLatex.js is dynamically imported by src/shell.js but also statically imported by src/math.js, vendor/jt-speech/index.js, dynamic import will not move module into another chunk.

[plugin builtin:vite-reporter]
(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Use build.rolldownOptions.output.codeSplitting to improve chunking: https://rolldown.rs/reference/OutputOptions.codeSplitting
- Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.
✓ built in 221ms
```

Exit: `0`.

Final combined gates (strict patch check, banned-copy scan, zero-network scan,
jt-web status/SHA, and `git diff --check`) produced:

```text
panel copy scan: 0 banned UI terms
demo network scan: 0 network calls or remote URLs
## main...origin/main
9d1611813854249d65353995474a7e31880c0447
9d1611813854249d65353995474a7e31880c0447
```

Exit: `0`.

## Final edge-case verification

The final review identified two more host edge cases. A return is now `handled`
only when a saved place is actually opened. A paired send is now `handled` only
when delivery succeeds and the content check matches; unverified delivery is
reported plainly as not completed.

Command:

```sh
git -C /Users/apple/projects/jt-web apply --check --whitespace=error-all /Users/apple/projects/jt-glass/patches/jt-web-tap.patch && npm run check && git diff --check
```

Verbatim output:

```text
> @jt/glass@0.0.0 check
> npm test && npm run build

> @jt/glass@0.0.0 test
> node --test

✔ the build produces a self-contained browser demo (135.586334ms)
✔ panel markup escapes session data and explains each visible area (0.773167ms)
✔ the exported mount heading escapes a caller-provided title (0.082458ms)
✔ the same fixture always produces the same panel state (9.360333ms)
✔ a changed match score is reported at its exact outcome path (2.550875ms)
✔ a recorder captures only the live events between start and stop (0.341625ms)
✔ a malformed JSONL line names the line and contract problem (0.764042ms)
✔ a recorder can update a live replay as each event arrives (0.174625ms)
✔ new interim words replace the previous changing hypothesis (0.203667ms)
✔ replay rejects mixed sessions even when called from plain JavaScript (1.527667ms)
✔ the tap contract accepts a complete transcript event (5.33625ms)
✔ the tap contract explains malformed match spans (0.324792ms)
✔ a tap publishes in order and records portable JSONL (0.817417ms)
✔ every event kind crosses the JSON boundary (0.360375ms)
✔ event times must include a canonical ISO time (0.086333ms)
ℹ tests 15
ℹ suites 0
ℹ pass 15
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 296.632959

> @jt/glass@0.0.0 build
> node --no-warnings scripts/build.mjs

built /Users/apple/projects/jt-glass/dist
```

Exit: `0`.

The exact final patch was also built successfully in its disposable applied
jt-web copy. Final output line: `✓ built in 388ms`. Exit: `0`.

## Rebase to current jt-web main

While final review was running, jt-web `main` advanced through the P1 verb-registry
merge. The integration was regenerated against the new registry-driven engine,
not left on the stale base. The current authority is
`7da8e677ddd7851b3f7f2cd9d35bf399a364cb7f`.

The regenerated patch was applied to a fresh archive of that SHA and the patched
copy completed a Vite production build: `✓ built in 310ms`, exit `0`.

Final gate command:

```sh
git -C /Users/apple/projects/jt-web apply --check --whitespace=error-all /Users/apple/projects/jt-glass/patches/jt-web-tap.patch && git -C /Users/apple/projects/jt-web status --short --branch && git -C /Users/apple/projects/jt-web rev-parse HEAD && npm run check && git diff --check
```

Verbatim output:

```text
## main...origin/main
7da8e677ddd7851b3f7f2cd9d35bf399a364cb7f

> @jt/glass@0.0.0 check
> npm test && npm run build

> @jt/glass@0.0.0 test
> node --test

✔ the build produces a self-contained browser demo (122.189167ms)
✔ panel markup escapes session data and explains each visible area (0.784917ms)
✔ the exported mount heading escapes a caller-provided title (0.086959ms)
✔ the same fixture always produces the same panel state (9.971875ms)
✔ a changed match score is reported at its exact outcome path (4.734833ms)
✔ a recorder captures only the live events between start and stop (0.407167ms)
✔ a malformed JSONL line names the line and contract problem (0.251542ms)
✔ a recorder can update a live replay as each event arrives (0.098292ms)
✔ new interim words replace the previous changing hypothesis (0.121417ms)
✔ replay rejects mixed sessions even when called from plain JavaScript (0.170458ms)
✔ the tap contract accepts a complete transcript event (2.423625ms)
✔ the tap contract explains malformed match spans (1.2895ms)
✔ a tap publishes in order and records portable JSONL (0.246625ms)
✔ every event kind crosses the JSON boundary (3.29325ms)
✔ event times must include a canonical ISO time (0.392625ms)
ℹ tests 15
ℹ suites 0
ℹ pass 15
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 301.433292

> @jt/glass@0.0.0 build
> node --no-warnings scripts/build.mjs

built /Users/apple/projects/jt-glass/dist
```

Exit: `0`.

## Independent final review and commit boundary

Independent review of the corrected current-main patch reported verbatim:

```text
Confirmed: no Critical or Important issues remain.
```

The completed-tree commit attempt was:

```sh
git diff --check && git add .gitignore README.md demo.ts docs fixtures index.html orders package.json patches receipts scripts src test && git commit -m 'feat: build jt glass engine v0'
```

Verbatim output:

```text
fatal: Unable to create '/Users/apple/projects/jt-glass/.git/index.lock': Operation not permitted
```

Exit: `128`. No index, branch, commit, or jt-web source was changed by this
failed operation. The complete implementation remains ready in the untracked
working tree on unborn `main`; creating the requested skeleton history and
`wo/p4-glass` branch requires write access to `.git`.
