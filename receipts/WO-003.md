# Receipt — WO-003

**Order:** `orders/WO-003-schemas.md`
**Date:** 2026-08-11
**Environment:** macOS, Node v24.13.1, npm 11.8.0, ajv ^8.20.0 + ajv-formats ^3.0.1

## What was delivered

- 11 record schemas in `schemas/` (draft 2020-12, `$id` = `jt-contracts/<record>/v0.1.0`,
  required `schemaVersion`, `additionalProperties: false`): Source, Anchor, Context,
  Cursor, Action, Receipt, Relationship, PlacePack, RightsRecord, CapabilityProfile,
  ProvenanceEnvelope.
- 22 golden fixtures (`fixtures/<record>/minimal.json` + `full.json`) and 11 invalid
  fixtures (`fixtures/invalid/<record>.json`).
- Conformance suite `test/validate.test.mjs` (`node --test`, ajv), lockfile committed.
- `README.md` (consumption + semver rule), `DECISIONS.md` (21 recorded judgment calls).

## Invalid-fixture intent

Each invalid fixture violates exactly one constraint, so a pass proves the schema
rejects it for the intended reason:

| Record | Deliberate violation |
|---|---|
| source | unknown extra property (`nickname`) |
| anchor | neither semantic nor geometric locator present |
| context | `privacyClass: "secret"` not in enum |
| cursor | `state: "floating"` not a state-machine value |
| action | missing required `consequenceClass` |
| receipt | `arrival: "probably-fine"` not in enum |
| relationship | endpoint `refType: "spirit"` not a thing-kind |
| placepack | missing required `locality` |
| rightsrecord | `modelTrainingExcluded: "yes"` is a string, not boolean |
| capabilityprofile | unknown `constraints.gpuScore` property |
| provenanceenvelope | `confidence: 1.5` above maximum 1 |

## Verify output

`npm test` (timings elided):

```text
> jt-contracts@0.1.0 test
> node --test

✔ all 11 portable records have a schema
✔ every schema declares $id, schemaVersion requirement, and closed properties
✔ source: golden fixtures validate
✔ source: invalid fixture is rejected
✔ anchor: golden fixtures validate
✔ anchor: invalid fixture is rejected
✔ context: golden fixtures validate
✔ context: invalid fixture is rejected
✔ cursor: golden fixtures validate
✔ cursor: invalid fixture is rejected
✔ action: golden fixtures validate
✔ action: invalid fixture is rejected
✔ receipt: golden fixtures validate
✔ receipt: invalid fixture is rejected
✔ relationship: golden fixtures validate
✔ relationship: invalid fixture is rejected
✔ placepack: golden fixtures validate
✔ placepack: invalid fixture is rejected
✔ rightsrecord: golden fixtures validate
✔ rightsrecord: invalid fixture is rejected
✔ capabilityprofile: golden fixtures validate
✔ capabilityprofile: invalid fixture is rejected
✔ provenanceenvelope: golden fixtures validate
✔ provenanceenvelope: invalid fixture is rejected
ℹ tests 24
ℹ suites 0
ℹ pass 24
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
```

Result: 24/24 pass. Valid fixtures accepted, invalid fixtures rejected, as intended.
Committed locally only; not pushed, no remote created.
