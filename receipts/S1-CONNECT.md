# Proof of work — S1-CONNECT

Repo: `/Users/apple/projects/jt-connectors`, branch `wo/s1-connect`. No remote; nothing pushed.

## Shipped

All five connectors, fully working and tested:

1. Plain text / markdown — `src/connectors/text.ts` (isomorphic)
2. PDF — `src/node/pdf.ts` (pdfjs-dist legacy build; per-page blocks, `page:N` locators, page count in provenance; hermetic fixture generated by `scripts/make-fixture-pdf.ts`)
3. URL → readable article — `src/node/url.ts` (@mozilla/readability + jsdom; tests use `test/fixtures/article.html` from disk and a loopback `127.0.0.1` server — no live network)
4. Clipboard/paste — `src/connectors/paste.ts` (isomorphic, dependency-free HTML cleaner in `src/core/html.ts`)
5. .docx — `src/node/docx.ts` (mammoth; hermetic fixture generated by `scripts/make-fixture-docx.ts`)

Cross-cutting: one `IngestResult` shape for all connectors, validated by the conformance
suite against `src/schema/ingestresult.schema.json` (draft 2020-12, jt-contracts
conventions); every connector has a malformed-input test; digests are sha-256 over the
exact input bytes and reproducibility is asserted per connector; CLI at `src/cli.ts`.

## Verify: full test run

Command:

```
npx vitest run
```

Output (verbatim, blank lines collapsed):

```
 RUN  v4.1.10 /Users/apple/projects/jt-connectors
 Test Files  6 passed (6)
      Tests  32 passed (32)
   Start at  16:03:06
   Duration  1.92s (transform 384ms, setup 0ms, import 3.50s, tests 865ms, environment 0ms)
```

Typecheck:

```
npx tsc --noEmit   # exits 0, no output
```

## Verify: CLI sample run on a real file

Command:

```
npx tsx src/cli.ts test/fixtures/sample.pdf
```

Output (verbatim):

```
{
  "schemaVersion": "0.1.0",
  "blocks": [
    {
      "text": "The first page of the jt fixture.\nIt exists so the PDF connector test is hermetic.",
      "index": 0,
      "kind": "page",
      "locator": "page:1"
    },
    {
      "text": "The second page carries different text.\nPage numbers must survive into the result.",
      "index": 1,
      "kind": "page",
      "locator": "page:2"
    }
  ],
  "provenance": {
    "sourceKind": "pdf",
    "contentDigest": "sha256:81d573570ea90f05d3140508048adbf19fa152ddaf43031b8116b357c79495c7",
    "byteSize": 1274,
    "capturedAt": "2026-08-12T10:33:24.791Z",
    "uri": "file:///Users/apple/projects/jt-connectors/test/fixtures/sample.pdf",
    "name": "sample.pdf",
    "title": "jt fixture: two page sample",
    "pageCount": 2
  },
  "warnings": []
}
```

Also exercised manually: `npx tsx src/cli.ts README.md` (markdown connector),
`npx tsx src/cli.ts test/fixtures/sample.docx` (docx), and
`npx tsx src/cli.ts test/fixtures/article.html` (article cleaner) — all print valid
IngestResults with digests and warnings.

## Blockers hit and resolved (depth ≤ 2)

- First `npm install` exceeded the 2-minute shell timeout → re-ran with a longer
  timeout; completed in 5s (registry cache warm). Resolved.
- pdfjs-dist v6: `doc.destroy()` gone → hold the loading task and call
  `loadingTask.destroy()`; `isEvalSupported` no longer a documented init parameter →
  dropped. Resolved.
- Readability kept a `class="ad"` div in the fixture (that class alone does not match
  its unlikely-candidates heuristics) → made the fixture chrome realistic
  (`aside.sidebar.banner.promo`), which Readability strips. Resolved.

## Open

Nothing blocking. Optional follow-ons: browser-side PDF entry (pdfjs non-legacy build),
size limits / timeouts on `ingestUrl`, character-offset locators inside blocks.
